// ══════════════════════════════════════════════════════════════
// TODO — Servicio de Valoraciones
// Ver instrucciones detalladas en src/controllers/valoraciones.controller.js
// ══════════════════════════════════════════════════════════════

const { pool, withTransaction } = require('../config/db');
const { AppError } = require('../middlewares/errorHandler');

// TODO: implementar crear(usuarioId, { intercambio_id, calificacion, comentario })
// TODO: implementar listarPorUsuario(usuarioId)

module.exports = {};
